services:
  - type: web
    name: desenvolva-lancamentos-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_desenvolva_lancamentos:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
